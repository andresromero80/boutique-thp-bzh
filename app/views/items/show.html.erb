<div>
  <a href="/carts/add_item/<%= @item.id %>" data-remote=true> Add to Cart </a>
  <div>
    <%= @item.title %>
    <%= @item.short_description %>
    <%= @item.price %>
    <img src="<%= @item.image_url %>"
  </div>
</div>